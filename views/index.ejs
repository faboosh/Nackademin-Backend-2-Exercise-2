<%- include('header'); -%>
    <h1>Välkommen till reddit 2.0</h1>
    <div class="blogContent">
        <% for(let i = 0; i < posts.length; i++) { %>
        <div id="<%= posts[i]._id %>">
            <h3> <%= posts[i].title %> </h3> 
            <p> <%= posts[i].content %> </p> 
            <a href="/viewPost/<%= posts[i]._id %>">View Post</a>
            <button onclick="deletePost('<%= posts[i]._id %>')">Delete</button>
        </div>
        <% } %>
    </div>
</body>
</html>